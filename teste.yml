version: '3'
services:
  zookeeper:
    image: confluentinc/cp-zookeeper:6.0.1
    ports:
      - 2181:2181
    environment:
      - ZOOKEEPER_CLIENT_PORT=2181
      - ZOOKEEPER_TICK_TIME=2000
    networks:
      - kafka-network
    hostname: zookeeper
    container_name: zookeeper

  kafka:
    image: confluentinc/cp-kafka:6.0.1
    hostname: kafka-server
    container_name: kafka-server
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      - KAFKA_BROKER_ID=1
      - KAFKA_ZOOKEEPER_CONNECT=zookeeper:2181
      - KAFKA_LISTENER_SECURITY_PROTOCOL_MAP=PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      - KAFKA_ADVERTISED_LISTENERS=PLAINTEXT://kafka-server:29092,PLAINTEXT_HOST://localhost:9092
      - KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR=1
      - KAFKA_AUTO_CREATE_TOPICS_ENABLE=true
    networks:
      - kafka-network

  mongo:
    image: mongo
    hostname: mongo-server
    container_name: mongo-server
    restart: always
    ports:
      - 27017:27017
    networks:
      - kafka-network
    environment:
      - MONGO_INITDB_ROOT_USERNAME=admin
      - MONGO_INITDB_ROOT_PASSWORD=1234
      - MONGO_INITDB_DATABASE=admin
      - MONGO_DB_USERNAME=admin
      - MONGO_DB_PASSWORD=1234

    #command: mongod --auth

  #advertisement-notification:
    #image: viniciussf/advertisement-notification:latest
    #hostname: advertisement-notification
    #container_name: advertisement-notification
    #ports:
     # - "8099:8099"
    #networks:
    #  - kafka-network
    #depends_on:
     # - kafka
     # - mongo

networks:
  kafka-network:
